'use strict';

export const initialState = {
  head: null,
  shoulders: null,
  chest: null,
  legs: null,
  feet: null
};

export default function reducer(state=initialState, action) {
  return state;
}
